# NinaVSI

(Currently in development as a part of my Master's Thesis)

VSI is the bridge between object detection (via machine learning) and video games. The screen is captured in real time and fed into the TF Java API which calls 
a model and runs inference on the image, displaying the confidence and bounding boxes on the screen. 
